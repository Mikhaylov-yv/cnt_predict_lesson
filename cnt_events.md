# Данные о событиях контейнеров

## Описание данных

**data/cnt_events.csv**:
   - `dt` — дата события.
   - `event` — тип события (например, "Прибыл_свободным", "Отправлен_по_ЖД").
   - `cnt_id` — идентификатор контейнера.
   - `cnt_type` — тип контейнера (20 или 40).
   - `location_name` — локация, где происходит событие.
   - `end_location_name` — конечная локация контейнера.

## Вопросы к данным

### Анализ и логика данных

1. Как создать набор данных с количеством свободных контейнеров в данный момент? Как составить его на весь период с шагом в 1 день?
2. Каким образом полученный набор будет обновляться?
3. Как он будет обновляться, если события записываются задним числом?
4. Как посчитать время в пути между `location_name` и `end_location_name` для контейнера?
5. Какие агрегаты можно построить для аналитики?

### Машинное обучение

1. Как можно формализовать задачу прогнозирования — это временной ряд или задача регрессии с признаками?
2. Как работать с локациями, в которые контейнеры редко отправляют (какие возникнут проблемы, как их решать)?
3. Как оценить качество прогноза для задачи с временной структурой данных — какие ML метрики подойдут?
4. Как выстроить ML-pipeline, если данные поступают постепенно и задним числом?
5. Как обнаружить аномалии в движении контейнеров и как их обрабатывать?
6. Какие внешние источники данных можно использовать для улучшения прогноза количества свободных контейнеров?